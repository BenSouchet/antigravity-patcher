export const IPC_CHANNELS = {
  snapshot: "snapshot",
  saveConfig: "save-config",
  runBlocks: "run-blocks",
  detectInstallPath: "detect-install-path",
  launchAntigravity: "launch-antigravity",
  openPortableRoot: "open-portable-root",
  reloadBlocks: "reload-blocks",
  runtimeEvent: "runtime-event"
} as const;
